# API base URL for the backend (e.g., http://localhost:4000/api)
REACT_APP_API_BASE=''

# Optional alternative backend URL variable if used elsewhere in your env
REACT_APP_BACKEND_URL=''

# Feature flags (comma-separated). Include USE_MOCK_API to force mock.
REACT_APP_FEATURE_FLAGS='USE_MOCK_API'
